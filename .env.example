# OpenAI API key for the chat LLM (embeddings are local and free)
OPENAI_API_KEY=sk-...

# Shared secret the n8n workflow must send in the X-Token header
# to trigger a re-scrape via POST /webhook/rescrape
RESCRAPE_TOKEN=change-me-to-a-random-string
